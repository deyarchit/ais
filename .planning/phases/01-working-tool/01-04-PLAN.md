---
phase: 01-working-tool
plan: 04
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
files_modified: []
autonomous: false
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
  - SRCH-01
  - SRCH-02
  - OUT-01
  - OUT-02
  - CFG-01
  - TOOL-01

must_haves:
  truths:
    - "`make build` produces ./bin/ais without errors"
    - "`ais -q 'query'` prints a glamour-rendered response with source citations and exits cleanly"
    - "`ais` opens an interactive REPL with `ais> ` prompt and no welcome header"
    - "Follow-up questions in chat mode reference earlier answers (multi-turn context preserved)"
    - "Source URLs appear after every response (or 'Sources: none' if none returned)"
    - "Ctrl+C and Ctrl+D exit the REPL cleanly"
  artifacts:
    - path: "./bin/ais"
      provides: "Compiled binary"
    - path: "cmd/ais/main.go"
      provides: "Fully wired entry point (no placeholders)"
    - path: "internal/gemini/client.go"
      provides: "Gemini client with grounding"
    - path: "internal/render/render.go"
      provides: "Shared rendering helpers"
    - path: "internal/repl/repl.go"
      provides: "REPL loop"
  key_links:
    - from: "./bin/ais -q flag"
      to: "Gemini API + glamour + sources"
      via: "runOneShot in main.go"
      pattern: "runOneShot"
    - from: "./bin/ais (no args)"
      to: "Gemini ChatSession (multi-turn)"
      via: "repl.Run in main.go"
      pattern: "repl\\.Run"
---

<objective>
End-to-end verification that both modes work correctly with a live Gemini API key. This is a human checkpoint — the executor verifies automated build checks pass, then the user confirms the tool behaves correctly in their terminal.

Purpose: Phase 1 success criteria require live API verification that cannot be automated (no test API key in CI). This checkpoint ensures all six ROADMAP success criteria are met before declaring Phase 1 complete.
Output: Confirmed working tool; Phase 1 marked complete.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-working-tool/01-CONTEXT.md
@.planning/phases/01-working-tool/01-02-SUMMARY.md
@.planning/phases/01-working-tool/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated build and static verification</name>
  <files></files>
  <read_first>
    - Makefile (confirm build target is `./cmd/ais`)
    - cmd/ais/main.go (confirm no placeholder strings remain)
  </read_first>
  <action>
Run all automated checks:

1. `make build` — must exit 0 and produce `./bin/ais`
2. `go vet ./...` — must exit 0 (no static analysis errors)
3. `go build ./...` — full module compile check
4. Verify no placeholder text remains:
   - `grep "placeholder" cmd/ais/main.go` — must return no matches
5. Verify key structural properties:
   - `grep 'ais> ' internal/repl/repl.go` — prompt string present
   - `grep 'GoogleSearch' internal/gemini/client.go` — grounding tool present
   - `grep 'WithAutoStyle' internal/render/render.go` — glamour style present
   - `grep 'Sources: none' internal/render/render.go` — fallback citation present
   - `ls -la ./bin/ais` — binary exists and is executable

If any check fails, diagnose and fix before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>cd /Users/deyarchit/Projects/ai/ais && make build && go vet ./... && ls -la ./bin/ais && echo "all static checks passed"</automated>
  </verify>
  <acceptance_criteria>
    - `make build` exits 0
    - `./bin/ais` exists as an executable file
    - `go vet ./...` exits 0
    - `grep "placeholder" cmd/ais/main.go` returns no output (no placeholder text)
    - `grep 'ais> ' internal/repl/repl.go` returns a match
    - `grep 'GoogleSearch' internal/gemini/client.go` returns a match
    - `grep 'Sources: none' internal/render/render.go` returns a match
  </acceptance_criteria>
  <done>All automated checks pass; binary is built and ready for live testing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live API verification — both modes end-to-end</name>
  <what-built>
Complete `ais` CLI tool with:
- One-shot mode: `ais -q "query"` — spinner, glamour-rendered response, numbered source URLs, clean exit
- Chat REPL: `ais` — `ais> ` prompt (no welcome), multi-turn conversation, same render/sources per turn, Ctrl+D exits cleanly
  </what-built>
  <how-to-verify>
With `GEMINI_API_KEY` set in your shell, run these tests in order:

**Test 1 — One-shot mode (MODE-01, OUT-01, OUT-02, SRCH-02):**
```
./bin/ais -q "what is the Go programming language?"
```
Expected:
- Spinner appears briefly while waiting
- Response is rendered with markdown formatting (headers, bold, code blocks visible — NOT raw `**text**`)
- A "Sources:" block appears below the response with numbered URLs (or "Sources: none")
- Process exits cleanly (shell prompt returns)

**Test 2 — Chat REPL basics (MODE-02, D-01, D-03):**
```
./bin/ais
```
Expected:
- `ais> ` prompt appears immediately, no welcome message
- Type a question (e.g., `what is Go?`) and press Enter
- Spinner, then rendered response, then sources
- `ais> ` prompt reappears for next query

**Test 3 — Multi-turn context (MODE-03):**
In the same REPL session from Test 2:
- First turn: `my favorite color is ultraviolet`
- Second turn: `what did I just tell you my favorite color is?`
Expected: Second response references "ultraviolet" — Gemini received the full conversation history

**Test 4 — Chat exit (D-02):**
- Press Ctrl+D — shell prompt returns, no error message
- Start again with `./bin/ais`, then press Ctrl+C — process terminates, no stack trace

**Test 5 — GEMINI_API_KEY env var (CFG-01):**
```
GEMINI_API_KEY= ./bin/ais -q "hello"
```
Expected: Error message mentioning `GEMINI_API_KEY` (exact wording may vary — Phase 2 polishes this)

Verify ALL five tests pass before typing "approved".
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe which tests failed and what you saw</resume-signal>
</task>

</tasks>

<verification>
Automated checks (Task 1) plus human live-API verification (Task 2) together confirm all Phase 1 ROADMAP success criteria:

1. `ais -q "query"` prints markdown-rendered answer with source URLs, then exits — verified in Test 1
2. `ais` (no args) opens interactive REPL accepting successive queries — verified in Test 2
3. Follow-up questions reference earlier answers (multi-turn context) — verified in Test 3
4. Responses visually rendered (headers, bold, code blocks) — verified in Test 1 and 2
5. Source URLs from grounding appear after each response — verified in Test 1 and 2
6. `make build` produces `./bin/ais` — verified in Task 1 automated checks
</verification>

<success_criteria>
- `make build` produces `./bin/ais` (TOOL-01)
- `ais -q "query"` works end-to-end with spinner, rendered markdown, and source citations (MODE-01, OUT-01, OUT-02, SRCH-02)
- `ais` opens REPL with `ais> ` prompt, no header, Ctrl+D exit (MODE-02, D-01, D-02, D-03)
- Multi-turn context verified live (MODE-03)
- All six Phase 1 ROADMAP success criteria confirmed by user
</success_criteria>

<output>
After completion, create `.planning/phases/01-working-tool/01-04-SUMMARY.md` documenting:
- Which tests passed
- Any issues found and fixed
- Final Phase 1 status

Then update `.planning/STATE.md`: set Phase 1 status to complete, update progress to 100%.
</output>
