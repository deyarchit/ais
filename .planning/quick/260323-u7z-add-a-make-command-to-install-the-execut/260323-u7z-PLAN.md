---
phase: quick
plan: 260323-u7z
type: execute
wave: 1
depends_on: []
files_modified:
  - Makefile
autonomous: true
requirements: []
must_haves:
  truths:
    - "Running `make install` copies the ais binary to /usr/local/bin/ais"
    - "The install target builds the binary first so it is always up to date"
  artifacts:
    - path: "Makefile"
      provides: "install target"
      contains: "install:"
  key_links:
    - from: "Makefile install target"
      to: "/usr/local/bin/ais"
      via: "cp ./bin/ais /usr/local/bin/ais"
---

<objective>
Add a `make install` target that builds the binary and copies it to /usr/local/bin so the user can run `ais` from any directory after a single command.

Purpose: Developer convenience — one command to build + install.
Output: Updated Makefile with `install` target.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add install target to Makefile</name>
  <files>Makefile</files>
  <action>
Add an `install` target immediately after the existing `build` target. The target must:
1. Run `build` first (via dependency) so the binary is always fresh
2. Copy `./bin/ais` to `/usr/local/bin/ais` using `cp`
3. Include a brief comment consistent with the existing comment style (## prefix)

Insert after line 4 (the build target block ends there):

```makefile
## install: build and install the binary to /usr/local/bin
install: build
	cp ./bin/ais /usr/local/bin/ais
```

The `.PHONY: *` at the bottom already covers all targets — no additional .PHONY line needed.
  </action>
  <verify>grep -n "install" /Users/deyarchit/Projects/ai/ais/Makefile</verify>
  <done>Makefile contains an `install` target that depends on `build` and runs `cp ./bin/ais /usr/local/bin/ais`</done>
</task>

</tasks>

<verification>
After the task completes, run:
  make install
and confirm `ais --help` works from a fresh shell (or run `/usr/local/bin/ais --help` directly to bypass PATH cache).
</verification>

<success_criteria>
- `make install` succeeds without errors
- `/usr/local/bin/ais` exists and is executable
- Running `ais` from any directory invokes the newly built binary
</success_criteria>

<output>
After completion, create `.planning/quick/260323-u7z-add-a-make-command-to-install-the-execut/260323-u7z-SUMMARY.md`
</output>
