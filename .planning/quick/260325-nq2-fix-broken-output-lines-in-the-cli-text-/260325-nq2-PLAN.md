---
phase: quick
plan: 260325-nq2
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/render/render.go
autonomous: true
requirements: []
must_haves:
  truths:
    - "Markdown output wraps at the actual terminal width"
    - "Output does not contain trailing whitespace padding from a mismatched hardcoded width"
    - "When stdout is piped or redirected (non-TTY), output wraps at 80 columns"
  artifacts:
    - path: "internal/render/render.go"
      provides: "Dynamic terminal-width word wrap for glamour renderer"
      contains: "term.GetSize"
  key_links:
    - from: "internal/render/render.go"
      to: "golang.org/x/term"
      via: "term.GetSize(int(os.Stdout.Fd()))"
      pattern: "term\\.GetSize"
---

<objective>
Replace the hardcoded `glamour.WithWordWrap(120)` in `internal/render/render.go` with a dynamic width queried from the actual terminal at render time. Fall back to 80 columns when stdout is not a TTY (piped or redirected output).

Purpose: Eliminates trailing whitespace padding and mismatched line breaks caused by the fixed 120-column wrap width when the terminal is narrower or wider.
Output: Updated `internal/render/render.go` with dynamic word wrap.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@internal/render/render.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace hardcoded word-wrap width with dynamic terminal width</name>
  <files>internal/render/render.go</files>
  <action>
    In `internal/render/render.go`:

    1. Add `"golang.org/x/term"` to the import block (it is already an indirect dependency — no `go get` needed).

    2. Before constructing the glamour renderer, detect the terminal width:

    ```go
    width := 80 // non-TTY fallback
    if w, _, err := term.GetSize(int(os.Stdout.Fd())); err == nil && w > 0 {
        width = w
    }
    ```

    3. Replace `glamour.WithWordWrap(120)` with `glamour.WithWordWrap(width)`.

    Query per-render call (do not cache) — terminal windows can be resized between renders and this is a trivially cheap syscall.

    The `term` package is `golang.org/x/term`, already present in go.mod as an indirect dependency. Import it directly; no module changes required.
  </action>
  <verify>
    <automated>cd /Users/deyarchit/Projects/ai/ais && go build ./... && go vet ./internal/render/...</automated>
  </verify>
  <done>
    `go build ./...` succeeds with no errors. `internal/render/render.go` imports `golang.org/x/term`, calls `term.GetSize` to derive wrap width, and passes that width to `glamour.WithWordWrap`. The hardcoded value `120` no longer appears in the file.
  </done>
</task>

</tasks>

<verification>
- `go build ./...` passes
- `go vet ./internal/render/...` passes
- `grep -n "WithWordWrap(120)" internal/render/render.go` returns no matches
- `grep -n "term.GetSize" internal/render/render.go` returns a match
</verification>

<success_criteria>
Running `ais` in a terminal narrower than 120 columns no longer produces lines padded to 120 characters. Piped output (`ais -q "hello" | cat`) wraps at 80 columns.
</success_criteria>

<output>
After completion, create `.planning/quick/260325-nq2-fix-broken-output-lines-in-the-cli-text-/260325-nq2-SUMMARY.md`
</output>
