---
phase: quick
plan: 260323-q65
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/ais/main.go
  - internal/repl/repl.go
autonomous: true
requirements: []

must_haves:
  truths:
    - "Running with --no-refs suppresses the Sources block in one-shot output"
    - "Running without --no-refs still shows Sources block (default unchanged)"
    - "REPL mode accepts --no-refs flag and suppresses Sources each turn"
  artifacts:
    - path: cmd/ais/main.go
      provides: "--no-refs flag parsed and threaded through runOneShot"
    - path: internal/repl/repl.go
      provides: "showRefs bool parameter controlling Sources call"
  key_links:
    - from: cmd/ais/main.go
      to: internal/repl/repl.go
      via: "Run(ctx, showRefs bool)"
      pattern: "repl\\.Run\\(ctx,.*\\)"
---

<objective>
Add a --no-refs flag that suppresses the Sources citation block from both one-shot (-q) and REPL output.

Purpose: Users who find the citations noisy or are piping output to other tools can opt out without losing the grounded answers.
Output: Both entry points (main.go, repl.go) respect a showRefs bool; when false, render.Sources is not called.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

Key design context:
- Flag-based mode switching already established: -q for one-shot, no args for REPL (STATE.md Decisions)
- render.Sources(urls []string) is the sole call site for printing citations (internal/render/render.go:35)
- Sources called in runOneShot (cmd/ais/main.go:82) and repl.Run loop (internal/repl/repl.go:79)
- repl.Run signature is currently: Run(ctx context.Context) error
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --no-refs flag and thread showRefs through both entry points</name>
  <files>cmd/ais/main.go, internal/repl/repl.go</files>
  <behavior>
    - `ais -q "hello"` → Sources block printed (default behavior unchanged)
    - `ais --no-refs -q "hello"` → Sources block suppressed, markdown response still printed
    - `ais --no-refs` (REPL) → Sources block suppressed every turn
    - `ais` (REPL, no flag) → Sources block printed every turn (unchanged)
  </behavior>
  <action>
In cmd/ais/main.go:
1. Add flag: `noRefs := flag.Bool("no-refs", false, "Suppress source citation block")`
   Place it immediately after the existing `query` flag declaration (line 19).
2. Pass to runOneShot: change signature to `runOneShot(ctx context.Context, query string, showRefs bool) error`
   and call it as `runOneShot(context.Background(), *query, !*noRefs)`.
3. Inside runOneShot, guard the Sources call:
   ```go
   if showRefs {
       render.Sources(gemini.ExtractSources(resp))
   }
   ```
4. Pass to REPL: change repl.Run call to `repl.Run(context.Background(), !*noRefs)`.

In internal/repl/repl.go:
1. Change Run signature to `Run(ctx context.Context, showRefs bool) error`.
2. Guard the Sources call inside the loop:
   ```go
   if showRefs {
       render.Sources(gemini.ExtractSources(resp))
   }
   ```

No changes needed to render.Sources or gemini.ExtractSources — the suppression is at the call site.
  </action>
  <verify>
    <automated>cd /Users/deyarchit/Projects/ai/ais && go build ./... && go vet ./...</automated>
  </verify>
  <done>
    `go build ./...` and `go vet ./...` both exit 0.
    `ais --help` shows --no-refs in flag list.
    Manually: `ais --no-refs -q "test"` produces response with no "Sources:" line.
  </done>
</task>

</tasks>

<verification>
go build ./... — clean build, no errors
go vet ./... — no vet warnings
ais --help — --no-refs flag appears in usage output
</verification>

<success_criteria>
`--no-refs` flag accepted by both one-shot and REPL modes; when set, render.Sources is not called; default behavior (Sources shown) is preserved when flag is absent.
</success_criteria>

<output>
After completion, create `.planning/quick/260323-q65-make-it-optional-to-show-references-in-t/260323-q65-SUMMARY.md`
</output>
